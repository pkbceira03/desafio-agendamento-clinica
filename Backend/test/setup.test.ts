import { describe, it, expect } from 'vitest';

describe('Setup Inicial', () => {
  it('deve validar que o ambiente de testes está configurado corretamente', () => {
    expect(1 + 1).toBe(2);
  });
});